@page
@model AwesomeApp.Web.Pages.ShoppingListDetails.IncompleteModel

<h2>Incomplete To Do Items (Razor Page)</h2>
<ul>
    @foreach (var item in Model.Items)
    {
        <li>
            @item.Title @if (item.IsDone)
        {
            <span>DONE!</span>
        }
        else
        {
        <span>NOT DONE</span>
        }
            <br />@item.Description
        </li>
    }
</ul>
